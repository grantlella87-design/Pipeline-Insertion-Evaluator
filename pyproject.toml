[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pipelineinsertion"
version = "0.1.0"
description = "LPP GSEP pipeline insertion candidate identification for MA Pressure View"
requires-python = ">=3.9"
dependencies = [
    "geopandas>=0.14",
    "pandas>=2.0",
    "shapely>=2.0",
    "pyproj>=3.5",
    "requests>=2.31",
    "keyring>=24.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["pipelineinsertion*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
